#include "dog.h"
#include <stdlib.h>

/**
 * struct dog - initializes dog
 * @d: pointer to struct
 * @name: SE
 * @age: SE
 * @owner: SE
 */
typedef struct dog
{
	if (d != NULL)
	{
		(*d).name = name;
		(*d).age = age;
		(*d).owner = owner;
	}
}
